---
import Tag from "@/components/Tag.astro";

interface Props {
  title: string;
  description: string;
  date: Date;
  tags?: string[];
}

const { title, description, date, tags } = Astro.props;
---

<article data-pagefind-body>
  <header class="mb-12">
    <h1 class="text-3xl font-bold mb-2">{title}</h1>
    <h2 class="text-lg mb-2">{description}</h2>
    <div class="flex gap-4 text-gray-600 text-sm">
      <time datetime={new Date(date).toISOString()}>
        {
          date.toLocaleDateString("en-US", {
            year: "numeric",
            month: "long",
            day: "numeric",
          })
        }
      </time>
    </div>
  </header>

  <div
    class="max-w-5xl mb-8
      prose prose-invert
      prose-h1:border-b prose-h1:pb-4
      prose-a:underline prose-a:underline-offset-4
      prose-p:text-neutral-300 prose-p:text-justify prose-img:rounded-xl
      prose-headings:text-neutral-300"
  >
    <slot />
  </div>

  <ul class="flex flex-wrap gap-2">
    {
      tags?.map((tag) => (
        <li>
          <Tag name={tag} variant="small" />
        </li>
      ))
    }
  </ul>
</article>
